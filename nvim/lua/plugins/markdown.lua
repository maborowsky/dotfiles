-- For `plugins/markview.lua` users.
return {
    -- Using obsidian for now
    enabled=true,
    "OXY2DEV/markview.nvim",
    lazy = false,

    -- For blink.cmp's completion
    -- source
    -- dependencies = {
    --     "saghen/blink.cmp"
    -- },
};
